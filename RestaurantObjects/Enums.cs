namespace RestaurantObjects
{
    public enum Status
    {
        On_Hold = 0,
        In_Progress = 1,
        Done = 2
    }

    public enum Discount
    {
        // In procents
        None = 0,
        Loyal_Customer = 25,
        Employee = 50,
        Special_Ocassion = 100
    }
}
